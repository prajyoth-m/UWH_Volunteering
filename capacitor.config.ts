import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'UWH_Volunteering',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
